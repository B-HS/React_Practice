export const Greet =(props) => {
    return (
    <div>
        <h1>Hello {props.name}</h1>
        {props.children}
    </div>)
}


/**
 * class == className 
 * for > htmlFor
 * naming change to CamelCase
 * 
 * */ 
